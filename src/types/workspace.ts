// Workspace types - placeholder for future implementation
export interface Workspace {
  id: string;
  name: string;
  slug?: string;
  avatarUrl?: string;
  createdAt: string;
  updatedAt: string;
}
