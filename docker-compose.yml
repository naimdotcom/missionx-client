services:
  missionx-client:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: missionx-client
    ports:
      - "80:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    networks:
      - missionx-network

networks:
  missionx-network:
    driver: bridge
